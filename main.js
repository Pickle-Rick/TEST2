var myName = "Chris's Apples";

var black = [0, 0, 0];
var black = [0, 0, 0];
var black = [0, 0, 0];
var blue = [0,0, 0];
var purple = [0, 0, 0];
var letterColors = [black, black, black, black, black];

drawName(myName, letterColors);

if(10 < 3)
{
    bubbleShape = 'square';
}
else
{
    bubbleShape = 'circle';
}

bounceBubbles();
